<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Color x:Key="PeachColor">#FFF8B26A</Color>
    <Color x:Key="SunColor">#FFFCD96A</Color>
    <Color x:Key="SkyColor">#FF9AD4FF</Color>
    <Color x:Key="LeafColor">#FF7ED4A8</Color>
    <Color x:Key="AccentColor">#FFEF6A6A</Color>
    <Color x:Key="PageColor">#FFFDF6E8</Color>

    <SolidColorBrush x:Key="PeachBrush" Color="{StaticResource PeachColor}" />
    <SolidColorBrush x:Key="PageBackgroundBrush" Color="{StaticResource PageColor}" />
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />

    <Style x:Key="CardBorderStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="28" />
        <Setter Property="Padding" Value="22" />
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="{StaticResource SunColor}" Offset="0" />
                    <GradientStop Color="{StaticResource SkyColor}" Offset="0.55" />
                    <GradientStop Color="{StaticResource LeafColor}" Offset="1" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PageTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="34" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <Style x:Key="SubtitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="Margin" Value="0,6,0,0" />
    </Style>

    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Background" Value="{StaticResource AccentBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Padding" Value="18,12" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="MinWidth" Value="150" />
        <Setter Property="MinHeight" Value="62" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="AnswerButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
        <Setter Property="Background" Value="{StaticResource PeachBrush}" />
        <Setter Property="Foreground" Value="#FF3A2A1A" />
        <Setter Property="Height" Value="110" />
    </Style>

    <Style x:Key="FriendlyTabControlStyle" TargetType="TabControl">
        <Setter Property="FontSize" Value="24" />
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <Style x:Key="BigEquationStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="54" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>

    <Style x:Key="MetricCardStyle" TargetType="Border">
        <Setter Property="Background" Value="White" />
        <Setter Property="CornerRadius" Value="22" />
        <Setter Property="Padding" Value="18" />
        <Setter Property="Margin" Value="8" />
    </Style>

    <Style x:Key="MetricLabelStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18" />
    </Style>

    <Style x:Key="MetricValueStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="34" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>
</ResourceDictionary>
